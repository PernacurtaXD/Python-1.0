import os 

os.system("cls || clear")

m = float(input("Digite quantos metros possue o objeto:"))

cm = m * 100
mm = m * 1000

print(f"Valor em metros = {m}\nConvertido em cm = {cm}\nConvertido em mm = {m} ")